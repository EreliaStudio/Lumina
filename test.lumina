#include <fileA>
#include "fileB.lum"

Input -> VertexPass : Vector2 modelPosition;
Input -> VertexPass : Vector2 modelUV;
VertexPass -> FragmentPass : Vector2 fragUV;

namespace MyNamespace
{
	namespace MyNamespace
	{
		Texture myTexture;
	}
}

struct Data
{
	int value;
	float test[2];
};

namespace spk
{
DataBlock myAttributeBlock as attribute
{
	int value;
	Data array[10 + VALUE];
};
}

DataBlock myConstantBlock
{
	int value;
	Data array[];
};

VertexPass()
{
    pixelPosition = Vector4(modelPosition, 0, 1);
	float dotValue = pixelPosition.distance(Vector4(0, 0, 0, 0));
    fragUV = modelUV;
}

FragmentPass()
{
    pixelColor = MyNamespace::MyNamespace::myTexture.getPixel(fragUV);
}
