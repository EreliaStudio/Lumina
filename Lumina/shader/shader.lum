Input -> VertexPass : Vector2 modelPosition;

ConstantBlock modelColor
{
	Color color;
};

VertexPass()
{
	pixelPosition = Vector4(modelPosition, 0, 1);
}

FragmentPass()
{
	pixelColor = modelColor.color;
}